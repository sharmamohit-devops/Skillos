version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"    # Frontend
      - "8000:8000"    # Backend API
    environment:
      - NODE_ENV=production
    volumes:
      - ./python-backend/data:/app/data
      - ./python-backend/logs:/app/logs
    restart: unless-stopped